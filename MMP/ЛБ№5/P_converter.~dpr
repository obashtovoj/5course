program P_converter;

uses
  Forms,
  U_main in 'U_main.pas' {F_main},
  U_child in 'U_child.pas' {F_child},
  U_Convert in 'U_Convert.pas' {F_conver};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TF_main, F_main);
  Application.CreateForm(TF_conver, F_conver);
  Application.Run;
end.
